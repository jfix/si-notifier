{
  "name": "si-notifier",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "start": "bun run src/server.ts",
    "check": "bun run src/check.ts"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "mqtt": "^5.14.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "^20.8.0"
  }
}